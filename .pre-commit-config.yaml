repos:
  - repo: local
    hooks:
      - id: check-utf8-no-bom
        name: Check UTF-8 Encoding (No BOM)
        entry: python scripts/check_encoding.py
        language: system
        types: [text]
        require_serial: true

